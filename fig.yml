send:
  build: .
  volumes:
  - ./send.conf:/etc/td-agent
  - ./in:/log

  ports:
  - "24224:24224"
  - "13337:13337"

  links:
  - recv:recv

recv:
  build: .
  volumes:
  - ./recv.conf:/etc/td-agent
  - ./out:/log
